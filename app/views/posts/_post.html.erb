<div id="<%= dom_id post %>">
  <p>
    <h4>Title: <%= post.title %></h4>
    <strong>Posted by : <%= link_to post.user.name, user_path(post.user) %></strong>
    <h6>
      <%= pluralize(post.views, "view")%>
    </h6>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>


</div>
